# Hackathon Backend

## Install
* Create virtual env
```shell
  $ python -m venv venv
  ```

* Install dependencies
```shell
  $ pip install -r requirements.txt
  ```

* Load environment variables
```shell
  $ set -a && source local.env
  ```

* Run app
```shell
  $ python main.py
  ```
